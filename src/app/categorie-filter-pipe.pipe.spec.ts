import { CategorieFilterPipePipe } from './categorie-filter-pipe.pipe';

describe('CategorieFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CategorieFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
